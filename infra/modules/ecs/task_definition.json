[
    {
      "essential": true,
      "name": "gpu-worker",
      "image": "${REPOSITORY_URL}:latest",
      "environment": [
        {
          "name": "AWS_DEFAULT_REGION",
          "value": "${REGION}"
        }
      ]
    }
  ]